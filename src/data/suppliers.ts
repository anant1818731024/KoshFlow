// Supplier and review records now live in the database and are served by the
// API (GET /api/suppliers, /api/suppliers/reviews), loaded through
// `useAppStore`. The canonical seed data is in `server/src/seed/suppliers.ts`.
// This file is intentionally left as a stub so the frontend carries no
// duplicate source of truth.
export {}
